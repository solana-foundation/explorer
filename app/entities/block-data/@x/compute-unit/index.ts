// Cross-import surface (FSD `@x`) exposing the block shape to the compute-unit entity, which
// summarizes a block's aggregate compute-unit totals.
export type { BlockWithV1 } from '../../model/types';
